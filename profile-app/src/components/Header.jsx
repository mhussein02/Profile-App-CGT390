export default function Header() {
  const title = "My Profile App";
  const tagline = "React + Vite Project";

  return (
    <header className="header">
      <h1>{title}</h1>
      <p>{tagline}</p>
    </header>
  );
}
